{
  "name": "@vrooli/prisma",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "dotenv -e ../../.env-dev -- prisma migrate dev --schema=../server/src/db/schema.postgres.prisma",
    "build": "tsc -b",
    "generate": "dotenv -e ../../.env-dev -- prisma generate --schema=../server/src/db/schema.postgres.prisma",
    "test": "echo \"No tests for prisma\""
  },
  "dependencies": {
    "@prisma/client": "6.1.0"
  },
  "devDependencies": {
    "prisma": "6.1.0",
    "typescript": "5.4.5"
  }
}
